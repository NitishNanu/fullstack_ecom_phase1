## Server (Express + Mongoose)

1. create `.env` copying `.env.example` and set MONGO_URI as needed.
2. npm install
3. npm run seed   # populate DB with 200 sample products
4. npm run dev    # start server with nodemon
